============================================================
[2026-09-11] Fixed "no images" + performance on old iMac
-----------------------------------------------------------
Modified: YankunFrameNative/main.swift
  - Changed launchServer() working directory from Bundle.main.resourcePath
    (inside .app bundle) to Bundle.main.bundleURL.deletingLastPathComponent()
    (parent of .app, i.e. ~/Desktop/YankunFrame/)
  - This fixes the bug where ./photos resolved inside the bundle and was empty

Modified: server.py
  - Changed HTTPServer → ThreadingHTTPServer (ThreadingMixIn)
    Slow RAW processing no longer blocks all other requests
  - Lowered default image processing: 4096x2304 → 1920x1080, q85 → q60
  - Added background pre-caching at startup (pre_cache_all thread)
  - Added config.json defaults updated to match

Modified: config.json
  - max_width: 4096 → 1920, max_height: 2304 → 1080, webp_quality: 85 → 60

Modified: static/index.html
  - Added loading overlay (#loading-overlay) with spinner + message

Modified: static/css/styles.css
  - Added .loading-overlay, .loading-spinner styles

Modified: static/js/app.js
  - Hides loading overlay when first image is shown
  - Shows error messages ("Cannot connect...", "No photos found...") in overlay

Created: AI_AGENT_GUIDE.md
  - Comprehensive guide for future AI agents working on this project
  - iMac connection info, architecture, files, troubleshooting, development workflow

NOTE: Must deploy to iMac AND recompile the .app (bash compile.sh) for Swift changes.
      Server + static changes take effect without recompilation (just restart app).
[2026-09-11] Random video start position
---------------------------------------------------
- Modified: static/js/preloader.js
  - Added _setupRandomStart(vid, displayInterval) helper function
    that listens for 'loadedmetadata' and sets currentTime to a
    random position, leaving at least displayInterval seconds
    before the end of the video
  - setCurrent(url, mediaType, volumeSettings, displayInterval):
    for videos, waits for metadata, seeks to random position,
    then calls play() (instead of playing immediately from 0:00)
  - preloadNext(url, mediaType, volumeSettings, displayInterval):
    pre-seeks preloaded video to random position so it's ready
    when swapped in
  - swap(nextUrl, nextType, volumeSettings, displayInterval):
    passes displayInterval through to internal preloadNext call
- Modified: static/js/app.js
  - showCurrent(): loads settings before calling setCurrent, passes
    settings.interval
  - scheduleNext(): loads settings before calling preloadNext, passes
    settings.interval
  - transition(): loads settings before calling swap, passes
    settings.interval
- Reason: Videos always started at 0:00, showing the same segment
  every time. Now each video clip starts at a random position,
  making the frame feel more dynamic. The random start is at
  least (interval) seconds before the video end, ensuring the
  full display duration plays through without clipping.

============================================================
YankunFrame - AI Changelog
============================================================

[2026-09-10] RE-FIX: Thread.sleep blocking main thread (app hang on launch)
---------------------------------------------------
- Modified: YankunFrameNative/main.swift
  - REMOVED: Thread.sleep(forTimeInterval: 2.0) — was blocking main thread,
    preventing window creation. Fix from earlier today was not persisted.
  - REORDERED: Window + WebView created FIRST, server launched after
  - Page load deferred via DispatchQueue.main.asyncAfter(2s) instead of blocking
  - Added NSApp.activate(ignoringOtherApps: true) for proper focus
- Reason: After Mac reboot, app bounced forever in Dock — main thread blocked
  by Thread.sleep before window was created. The fix documented in the
  previous changelog entry had not been actually applied to the file.

[2026-09-10] Sleep prevention for native macOS app
---------------------------------------------------
- Modified: YankunFrameNative/main.swift
  - Added import IOKit.pwr_mgt for sleep assertion API
  - Added sleepAssertionID property to AppDelegate
  - Added preventSleep() method using IOPMAssertionCreateWithName
  - Added applicationWillTerminate() to release assertion on quit
  - Called preventSleep() after server launch
- Reason: Prevent Mac display from sleeping while displaying photos

[2026-09-10] Fixed app launch hang + full SCP deployment + rebuild
---------------------------------------------------
- Modified: YankunFrameNative/main.swift
  - REMOVED: Thread.sleep(forTimeInterval: 2.0) — was blocking main thread, preventing window creation
  - REORDERED: window + WebView created FIRST, server launched, sleep prevention called
  - Page load now deferred via DispatchQueue.main.asyncAfter (2s delay) instead of blocking sleep
  - Added NSApp.activate(ignoringOtherApps: true) for proper focus
  - Fixed: kIOPMAssertionTypePreventUserIdleDisplaySleep as CFString (type cast)
  - Fixed: LF line endings on main.swift, compile.sh, run.sh, YankunFrame.command
- Transferred: Full project (17 files) to yank@imac-de-yank.local:/users/yank_imac/Desktop/YankunFrame/
- Rebuilt: YankunFrame.app on iMac (84 KB, works now)
- Reason: App was bouncing forever in Dock — main thread blocked by Thread.sleep
